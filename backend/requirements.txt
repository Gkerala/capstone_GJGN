Django==4.2
djangorestframework==3.14.0

# JWT 인증 (Python 3.8~3.12 호환)
djangorestframework-simplejwt==4.8.0

# CORS — 실제 존재하는 버전
django-cors-headers==4.3.1

requests==2.31.0
Pillow==10.0.0
python-dotenv==1.0.1

# YOLO / AI 라이브러리 (Windows + CPU 환경 호환)
ultralytics==8.0.130

# PyTorch (Windows 공식 안정 조합)
torch==2.0.1
torchvision==0.15.2

python-decouple
python-dotenv

requests
